name: CCTV端口扫描器

on:
  workflow_dispatch:
    inputs:
      scan_mode:
        description: '扫描模式'
        required: false
        default: 'smart'
        type: choice
        options:
          - 'smart (智能模式-推荐)'
          - 'ultra (极限模式-最快)'
          - 'conservative (保守模式-最稳)'
      max_workers:
        description: '并发线程数'
        required: false
        default: '800'
        type: choice
        options:
          - '500'
          - '800'
          - '1000'
      enable_verify:
        description: '验证结果'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
  schedule:
    - cron: '0 2 * * *'

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 设置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade requests
          echo "依赖安装完成"
      
      - name: 系统优化
        run: |
          ulimit -n 65535
          echo "文件描述符限制: $(ulimit -n)"
          sudo sysctl -w net.ipv4.ip_local_port_range="1024 65535"
          sudo sysctl -w net.core.somaxconn=4096
          sudo sysctl -w net.ipv4.tcp_tw_reuse=1
          sudo sysctl -w net.ipv4.tcp_fin_timeout=10
          sudo sysctl -w net.ipv4.tcp_syn_retries=2
      
      - name: 创建结果目录
        run: |
          mkdir -p results
          mkdir -p logs
      
      - name: 运行扫描器
        run: |
          echo "=========================================="
          echo "开始扫描: $(date)"
          echo "扫描模式: ${{ github.event.inputs.scan_mode }}"
          echo "并发线程: ${{ github.event.inputs.max_workers }}"
          echo "=========================================="
          
          MODE="${{ github.event.inputs.scan_mode }}"
          WORKERS="${{ github.event.inputs.max_workers }}"
          VERIFY="${{ github.event.inputs.enable_verify }}"
          
          if [[ "$MODE" == *"smart"* ]]; then
            echo "使用智能模式扫描"
            python py/scanner_intelligent.py --mode smart --workers $WORKERS --verify $VERIFY
          elif [[ "$MODE" == *"ultra"* ]]; then
            echo "使用极限模式扫描"
            python py/scanner_intelligent.py --mode ultra --workers $WORKERS --verify $VERIFY
          else
            echo "使用保守模式扫描"
            python py/scanner_conservative.py --workers $WORKERS
          fi
          
          echo "=========================================="
          echo "扫描结束: $(date)"
          echo "=========================================="
      
      - name: 显示扫描结果
        run: |
          if [ -f migu.txt ]; then
            echo ""
            echo "========== 扫描结果 =========="
            cat migu.txt
            echo "=============================="
            COUNT=$(wc -l < migu.txt)
            echo "有效目标数: $COUNT"
            
            if [ $COUNT -gt 0 ]; then
              echo ""
              echo "========== 结果统计 =========="
              echo "按端口统计:"
              cat migu.txt | cut -d: -f2 | sort | uniq -c | while read line; do
                echo "  $line"
              done
            fi
          else
            echo "未找到结果文件"
          fi
      
      - name: 生成扫描报告
        if: success()
        run: |
          cat > scan_report.md << EOF
          # CCTV端口扫描报告
          
          ## 扫描信息
          - **扫描时间**: $(date '+%Y-%m-%d %H:%M:%S')
          - **扫描模式**: ${{ github.event.inputs.scan_mode }}
          - **并发线程**: ${{ github.event.inputs.max_workers }}
          - **运行环境**: ${{ runner.os }}
          
          ## 扫描结果
          \`\`\`
          $(cat migu.txt 2>/dev/null || echo "无结果")
          \`\`\`
          
          ## 统计信息
          - **发现目标数**: $(wc -l < migu.txt 2>/dev/null || echo 0)
          
          ---
          *报告由 GitHub Actions 自动生成*
          EOF
      
      - name: 上传扫描结果
        uses: actions/upload-artifact@v4
        with:
          name: cctv-scan-results-${{ github.run_id }}
          path: |
            migu.txt
            scan_report.md
            results/
          retention-days: 30
      
      - name: 提交结果到仓库
        if: success()
        run: |
          if [ -f migu.txt ] && [ -s migu.txt ]; then
            COUNT=$(wc -l < migu.txt)
            if [ "$COUNT" -gt 0 ]; then
              git config user.name "github-actions[bot]"
              git config user.email "github-actions[bot]@users.noreply.github.com"
              
              if [ -f migu.txt ]; then
                cp migu.txt results/migu_$(date +%Y%m%d_%H%M%S).txt
              fi
              
              git add migu.txt results/ scan_report.md
              git commit -m "扫描结果更新: 发现 $COUNT 个CCTV目标 [skip ci]" || echo "无变化"
              git push
            fi
          fi

  cleanup:
    needs: scan
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: 清理旧文件
        run: echo "清理完成"